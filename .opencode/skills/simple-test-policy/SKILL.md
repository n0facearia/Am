---
name: simple-test-policy
description: Use before reporting any build/tweak task as finished.
---
Before calling anything done:
(1) Run the real build/compile command and confirm no errors, paste the output.
(2) Start the real app, confirm it runs.
(3) Actually exercise the specific thing just built/changed and show the real output.

If any step fails, report the failure, not success.
